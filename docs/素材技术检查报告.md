# 海河少女素材技术检查报告

检查日期：2026-09-13

## 三维工程

- `海河少/model01.vroid` 是有效 VRoid Studio 编辑工程。
- 工程元数据版本为 2，最近保存程序版本为 VRoid Studio 2.14.0。
- 工程内包含 `v1model/data.bin` 和 1024×1024 缩略图。
- `.vroid` 不是网页可直接加载的 VRM；必须在 VRoid Studio 导出 `haihe-girl.vrm`。

## 过程视频

| 文件 | 编码 | 尺寸 | 帧率 | 时长 | 用途判定 |
|---|---|---:|---:|---:|---|
| `1.mp4` | HEVC | 720×1040 | 30fps | 1.97秒 | 仅作为人物制作过程证据 |
| `2.mp4` | HEVC | 720×1040 | 30fps | 14.87秒 | 仅作为人物制作过程证据 |

两段视频均不符合最终演示的横屏分辨率、编码和时长要求，不能改名后直接提交。

## 最终视频标准

- MP4 封装，H.264 视频，AAC 音频。
- 1920×1080，30fps，时长 180–300 秒。
- 用最终 VRM、生产索引和百炼配置重新录制。
- 执行 `./scripts/prepare_final_video.sh <原始录屏>` 转码，再执行 `./scripts/check_submission.sh` 验证。
