# 最终演示视频

完成 VRM 导出、百炼正式配置和端到端验收后，按 `docs/演示视频脚本.md` 录制原始屏幕。不要把现有的低清 HEVC 过程片段冒充为最终演示。

录制后执行：

```bash
./scripts/prepare_final_video.sh /path/to/raw-screen-recording.mov
```

输出文件必须为 `海河少女功能演示.mp4`，1920×1080、H.264、3–5 分钟。
